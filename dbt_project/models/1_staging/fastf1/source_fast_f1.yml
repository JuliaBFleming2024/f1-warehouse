sources:
  - name: fastf1_dataset
    description: Connection to neon serverless postgres database fast_f1 tables
    tables:
      - name: schedule
        database: raw
        description: >
          Records of F1 season events.
      - name: race_results
        database: raw
        description: >
          Records of F1 race results per driver.
        
